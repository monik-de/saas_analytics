version: 2

models:
  - name: int_product_usage_monthly
    columns:
      - name: account_month_key
        data_tests: [not_null, unique]
      - name: month_start
        data_tests: [not_null]
      - name: account_id
        data_tests: [not_null]

  - name: int_support_monthly
    columns:
      - name: account_month_key
        data_tests: [not_null, unique]
      - name: month_start
        data_tests: [not_null]
      - name: account_id
        data_tests: [not_null]

  - name: int_invoices_monthly
    columns:
      - name: account_month_key
        data_tests: [not_null, unique]
      - name: month_start
        data_tests: [not_null]
      - name: account_id
        data_tests: [not_null]

  - name: int_revenue_movements_monthly
    columns:
      - name: account_month_key
        data_tests: [not_null, unique]
      - name: month_start
        data_tests: [not_null]
      - name: account_id
        data_tests: [not_null]

  - name: int_account_monthly
    columns:
      - name: account_month_key
        data_tests: [not_null, unique]
      - name: month_start
        data_tests: [not_null]
      - name: account_id
        data_tests: [not_null]